rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All reads and writes go through the backend (Admin SDK).
    // Direct client access is intentionally denied.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
